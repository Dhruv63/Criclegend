import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFD32F2F); // Red
  static const Color primaryDark = Color(0xFF9A0007);
  static const Color primaryLight = Color(0xFFFF6659);

  static const Color secondary = Color(
    0xFF009688,
  ); // Teal for actions like "Start Match"
  static const Color secondaryDark = Color(0xFF00695C);

  static const Color background = Color(0xFFF5F5F5); // Light Grey
  static const Color surface = Colors.white;
  static const Color surfaceContainer = Color(
    0xFFEEEEEE,
  ); // Slightly darker for cards on white

  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);

  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFD32F2F);
  static const Color warning = Color(0xFFFFA000);

  // New Premium Colors
  static const Color deepTeal = Color(0xFF00695C);
  static const Color premiumRed = Color(0xFFB71C1C);
}
